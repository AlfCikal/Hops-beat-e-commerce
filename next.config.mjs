/** @type {import('next').NextConfig} */
const nextConfig = {
	// output: "export",
	// basePath: "/Hops-beat-e-commerce", // Replace <repository-name> with your actual GitHub repository name
	// assetPrefix: "/Hops-beat-e-commerce", // Ensure static files are served from the correct path
	// reactStrictMode: true,
};

export default nextConfig;
